import { View, Text } from 'react-native'
import React from 'react'

export default function Character({ obj }) {
  console.log(obj)
  return (
    <View>
      <Text>Character</Text>
    </View>
  )
}